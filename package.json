{
  "name": "pause-plus",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: mindful pause before distracting sites, with schedules and focus boosts.",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
